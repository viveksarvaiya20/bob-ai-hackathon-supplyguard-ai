# Repository setup

GitHub repository name:

`bob-ai-hackathon-supplyguard-ai`

Suggested GitHub remote:

`https://github.com/<YOUR_GITHUB_USERNAME>/bob-ai-hackathon-supplyguard-ai.git`

Before pushing:
1. Fill the placeholders in `submission.yaml`.
2. Review `.env.example` and create your own local `.env`.
3. Never commit API keys, passwords, or other secrets.
4. Add a public demo-video URL if the hackathon requires one.
